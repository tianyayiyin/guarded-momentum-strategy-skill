{
  "name": "guarded-momentum-strategy",
  "version": "0.1.0",
  "description": "Generates a risk-limited momentum strategy from CMC-style market data.",
  "track": "Strategy Skills",
  "inputs": {
    "symbol": "string",
    "candles": "Array<{ timestamp: string, open: number, high: number, low: number, close: number, volume: number }>",
    "sentimentScore": "number between -1 and 1"
  },
  "outputs": {
    "action": "BUY | HOLD | REDUCE",
    "confidence": "number between 0 and 1",
    "maxPositionFraction": "number",
    "stopLossPct": "number",
    "takeProfitPct": "number",
    "rationale": "string"
  },
  "riskControls": [
    "No live execution by default",
    "Position size capped by volatility and drawdown",
    "Negative sentiment blocks new long entries",
    "Drawdown over 8% forces REDUCE"
  ]
}
